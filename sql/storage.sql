-- Storage buckets (execute in Supabase SQL editor)
-- Create buckets in Storage UI: quality-media, avatars

-- Policies are set in Storage Policies UI; below are suggested SQL policies (if using storage.objects)
-- Note: Supabase recommends configuring these via dashboard; keep this as reference.

-- Allow public read for quality-media
-- Allow admin write for quality-media
-- Allow users write/read their own avatars
